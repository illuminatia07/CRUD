<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="styles.css" rel="stylesheet">
</head>
<body>
   <!-- Navbar -->
   <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-nav d-flex justify-content-center">
        <a class="navbar-brand nav-cont" href="#">
            <% if (username) { %>
                <h1><%= username %>'s Dashboard</h1>
            <% } else { %>
                <h1>Welcome to our Website</h1>
            <% } %>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item d-flex justify-content-center moveup">
                    <a class="nav-link active font" aria-current="page" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Services</a>
                </li>
            </ul>
            <form class="d-flex">
                <input class="form-control me-2 search" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-light" type="submit">Search</button>
            </form>
            <% if (username) { %>
            <form action="/logout" method="POST" class="d-flex ">
                <button class="btn btn-danger move-right" type="submit">Logout</button>
            </form>
            <% } %>
        </div>
    </div>
</nav>

    <div class="container mt-5">
      <% if (username) { %>
      <h1>Welcome to our Website, <%= username %></h1>
      <% } else { %>
      <h1>Welcome to our Website</h1>
      <% } %>
      <div class="row mt-4">
        <div class="col-md-12 pt-4 f-start">
          <div class="card">
            <img src="/Img/card_img1.jpg" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">Frontend Design</h5>
              <p class="card-text">
                Frontend Design using Figma, HTML, CSS
              </p>
            </div>
          </div>
        </div>
        <div class="col-md-12 pt-4 f-start">
          <div class="card">
            <img src="/Img/card_img2.jpg" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">Backend</h5>
              <p class="card-text">
                Backend building using NodeJS, Express and MongoDB
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
